qemu-system-arm -no-reboot -M vexpress-a9 -m 128 -serial stdio -pflash os-flash.img

or

qemu-system-arm -no-reboot -M vexpress-a9 -m 128 -serial stdio -kernel os-kern.bin

Can run -kernel either os-flash.elf, os-kern.elf, or os-kern.bin.
Can run -pflash with os-flash.img

CPU: cortex-a9
Flash size: 64 MB (-pflash file must be exactly this size)
UART: pl011
